# API documentation
FastAPI serves `/docs` and `/redoc`. The design-time placeholder is `packages/contracts/openapi/openapi.yaml`. Starter read-only routes are `/api/v1/health`, `/overview`, `/partners`, `/shipments`, `/production-events`, and `/alerts`. Finalize pagination, filters, errors and identity claims with real requirements.
