# Infineon Supply Chain Command Center

Standalone local prototype exported from Claude. It uses local mock data only and
does not require an API, database, Docker, or dependency installation.

## Run locally

```bash
npm run dev
```

Then open <http://localhost:5173>.

Stop the server with `Ctrl+C`.
